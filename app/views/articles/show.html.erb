<div class="article-background sticky-top" data-controller="reveal" data-reveal-hidden-class="text-secondary">
  <div class="container container-md article col-6 p-5">
    <% if @article.source.present? %>
      <p><%= @article.source %></p>
    <% else %>
      <p>Placeholder source</p>
    <% end %>
    <div class="article-image" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(https://images.unsplash.com/photo-1584931423298-c576fda54bd2?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=870&q=80)">
    </div>
    <h2 class="title mt-2"><%= @article.title %></h2>
    <ul class="list-inline">
      <% @article.topics.each do |topic| %>
        <li class="list-inline-item">
          <p><span class="badge">
            <%= link_to toggle_favorite_topic_path(topic), data: {"turbo-method": :patch, action: "click->reveal#toggle"} do %>
              <% if current_user && current_user.favorited?(topic)%>
                <i data-reveal-target="item" class="fa-solid fa-thumbtack" style="color: #26630c;"></i>
              <% else %>
                <i data-reveal-target="item" class="fa-solid fa-thumbtack text-secondary" style="color: #26630c;"></i>
              <% end %>
            <% end %>
          </span>
          <%= topic.name %></p>
        </li>
      <% end %>
    </ul>
    <p class="content"><%= @article.content %></p>
    <p>Read the original article <%= link_to "here", @article.url, class: "source-link" %></p>
  </div>
</div>
